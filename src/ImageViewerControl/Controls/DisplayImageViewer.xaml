<UserControl x:Class="ImageViewer.Controls.DisplayImageViewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Focusable="True"
             ClipToBounds="True"
             Background="#0A0F16">
    <Grid x:Name="rootGrid" Background="#0A0F16">
        <Canvas x:Name="world">
            <Canvas.RenderTransform>
                <TransformGroup>
                    <ScaleTransform x:Name="worldScale" ScaleX="1" ScaleY="1" />
                    <TranslateTransform x:Name="worldTranslate" X="0" Y="0" />
                </TransformGroup>
            </Canvas.RenderTransform>
            <Image x:Name="image"
                   Stretch="None"
                   SnapsToDevicePixels="True"
                   RenderOptions.BitmapScalingMode="NearestNeighbor" />
            <ItemsControl x:Name="markerHost" IsHitTestVisible="False">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemContainerStyle>
                    <Style TargetType="ContentPresenter">
                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                    </Style>
                </ItemsControl.ItemContainerStyle>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="-4,-10,0,0">
                            <Ellipse Width="8" Height="8" Fill="Orange" Stroke="White" StrokeThickness="1" />
                            <TextBlock Text="{Binding Label}"
                                       Foreground="Orange"
                                       FontSize="12"
                                       FontWeight="SemiBold"
                                       Margin="4,-2,0,0" />
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Canvas>
    </Grid>
</UserControl>
