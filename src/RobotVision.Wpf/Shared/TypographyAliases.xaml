<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- 在覆盖同名键之前，先保存 WPF-UI 库内排版样式，避免 TypographyOverrides 自引用 BasedOn） -->
    <Style x:Key="__CaptionTextBlockStyle" TargetType="TextBlock"
           BasedOn="{StaticResource CaptionTextBlockStyle}" />
    <Style x:Key="__BodyTextBlockStyle" TargetType="TextBlock"
           BasedOn="{StaticResource BodyTextBlockStyle}" />
    <Style x:Key="__BodyStrongTextBlockStyle" TargetType="TextBlock"
           BasedOn="{StaticResource BodyStrongTextBlockStyle}" />
    <Style x:Key="__SubtitleTextBlockStyle" TargetType="TextBlock"
           BasedOn="{StaticResource SubtitleTextBlockStyle}" />
    <Style x:Key="__TitleTextBlockStyle" TargetType="TextBlock"
           BasedOn="{StaticResource TitleTextBlockStyle}" />
    <Style x:Key="__TitleLargeTextBlockStyle" TargetType="TextBlock"
           BasedOn="{StaticResource TitleLargeTextBlockStyle}" />
    <Style x:Key="__DisplayTextBlockStyle" TargetType="TextBlock"
           BasedOn="{StaticResource DisplayTextBlockStyle}" />

</ResourceDictionary>
