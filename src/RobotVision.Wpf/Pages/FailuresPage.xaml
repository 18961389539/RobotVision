<Page x:Class="RobotVision.WpfHost.Pages.FailuresPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      xmlns:ivc="clr-namespace:ImageViewer.Controls;assembly=ImageViewerControl"
      Title="失败现场"
      Background="{DynamicResource ApplicationBackgroundBrush}">
    <Page.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}" />
    </Page.InputBindings>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:Card Grid.Row="0" Margin="0,0,0,8" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:TextBlock FontTypography="BodyStrong" Text="失败现场留存" VerticalAlignment="Center" />
                <TextBlock Grid.Column="1" Text="{Binding Message}" Margin="16,0,0,0"
                           VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                <ui:Button Grid.Column="2" Content="刷新" ToolTip="F5" Command="{Binding RefreshCommand}" />
                <ui:Button Grid.Column="3" Content="打开目录" Margin="8,0,0,0"
                           Command="{Binding OpenFolderCommand}" />
                <ui:Button Grid.Column="4" Content="删除选中" Margin="8,0,0,0"
                           Command="{Binding DeleteSelectedCommand}" />
                <ui:Button Grid.Column="5" Content="{Binding DeleteAllButtonText}" Margin="8,0,0,0"
                           Command="{Binding DeleteAllCommand}" />
            </Grid>
        </ui:Card>

        <!-- 筛选：按配方 / 错误码 -->
        <ui:Card Grid.Row="1" Margin="0,0,0,8" Padding="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="160" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="120" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <TextBlock Text="配方" VerticalAlignment="Center" />
                <ComboBox Grid.Column="1" ItemsSource="{Binding RecipeFilters}"
                          SelectedItem="{Binding RecipeFilter}" Margin="8,0,16,0" />
                <TextBlock Grid.Column="2" Text="错误码" VerticalAlignment="Center" />
                <ComboBox Grid.Column="3" ItemsSource="{Binding ErrorCodeFilters}"
                          SelectedItem="{Binding ErrorCodeFilter}" Margin="8,0,16,0" />
                <TextBlock Grid.Column="4" Text="{Binding FilterSummary}" VerticalAlignment="Center"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            </Grid>
        </ui:Card>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- 左：缩略图列表（Card 模板垂直居中内容，改用 Border 填满高度） -->
            <Border Grid.Column="0" Margin="0,0,8,0" Padding="8"
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="4">
                <ListBox ItemsSource="{Binding Items}" SelectedItem="{Binding Selected}"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel />
                        </ItemsPanelTemplate>
                    </ListBox.ItemsPanel>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Width="118" Margin="2">
                                <Border Background="#1E1E1E" CornerRadius="3">
                                    <Image Source="{Binding Thumb}" Stretch="Uniform" Height="82" />
                                </Border>
                                <TextBlock Text="{Binding DisplayName}" FontSize="10"
                                           Foreground="{DynamicResource TextFillColorTertiaryBrush}"
                                           TextTrimming="CharacterEllipsis" Margin="0,3,0,0" />
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>

            <!-- 右：详情 -->
            <Border Grid.Column="1" Padding="8"
                    Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1" CornerRadius="4">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>
                    <TextBlock Text="{Binding Selected.MetaText}" Margin="4,0,4,8"
                               TextWrapping="Wrap"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                    <ivc:ImageViewer Grid.Row="1" ImageSource="{Binding PreviewImage}"
                                     IsToolbarVisible="False" />
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
